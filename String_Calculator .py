# at Initial commit of code

def add(num_str):
  def add(num_str):
  # code for test case 3
  Sum = 0
  for i in range(len(arr)):
    if arr[i].isdigit():
      if int(arr[i]) > 1000:
        continue
      else:
        Sum += int(arr[i])
  return Sum
    
